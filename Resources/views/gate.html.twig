<!doctype html>
<html>
	<head>
		<meta charset="utf-8">
		<title>Gate</title>
		<style>
		</style>
	</head>
	<body>
		{% if true %}{# if we are in password mode #}
		<div id="wrapper">
			<form name="access" action="/"> {# the url doesn't matter #}
				<label>Password: </label><input type="text" name="password">
			</form>
		</div>
		{% endif %}
	</body>
</html>